server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Yassinean/BankApp-MicroService.git
          search-paths:
            - centralized-config